# Playwright test artifacts (regenerable)
playwright-report/
test-results/
blob-report/

# Playwright traces (if saved outside defaults)
*.trace.zip
traces/

# Coverage outputs (when enabled)
coverage/
.nyc_output/

# Storybook static build (if you generate it locally)
# storybook-static/
